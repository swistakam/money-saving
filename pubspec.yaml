name: money_saving
description: Praca inżynierska

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^5.0.0  # Przesyłanie danych między oknami
  # charts_flutter: ^0.10.0 # Graficzne przedstawienie dostępnych danych w wykresy
  # fl_chart: ^0.36.0 # Graficzne przedstawienie dostępnych danych w wykresy
  pie_chart: ^5.0.0
  firebase_core: ^1.1.0
  firebase_auth: ^1.1.3
  modal_progress_hud_nsn: ^0.1.0-nullsafety-1 # Kręcący się spiner w oczekiwaniu na decyzje
  firebase_database: ^6.1.2
  cloud_firestore: ^2.0.0
  percent_indicator: ^3.0.1 # progres bar do wyznaczania celów
  intl: ^0.17.0 # zarządzanie czasem
  

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - images/
